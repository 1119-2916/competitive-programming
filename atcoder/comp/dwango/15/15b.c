#include <stdio.h>
#include <string.h>

int main(void) {
    char inp[100001], nico[3] = {"25"};
    int len;
    scanf("%s",inp);
    len = strlen(inp);
    for(i = 0; i < len; i++) {
        if(inp[i] == '2' && inp[i+1] == '5') {
           
    
