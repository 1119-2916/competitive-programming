#include <stdio.h>

char input[101];

int main(void)
{
    int i, j, n, len, count;
    scanf("%d",&n);
    scnaf("%s",input);
    for (k = 0; k < n; k++) {
        i = 1;
        len = strlen(input);
        befoer = input[0];
        count = 1;
        while (i < len) {
            if (input[i] == before) {
                count++;
            } else {
                add
            }



